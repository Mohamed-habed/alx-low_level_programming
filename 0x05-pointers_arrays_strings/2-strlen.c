#include "main.h"
/**
*_strlen - Returns the  length of a string.
*@s: String to be measured.
*
*Return: Length of the string.
*/
int strlen(char *s)
{
int length = 0;
while (*s != '10')
{ length++;
S++;
return (length);
}
